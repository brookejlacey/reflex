{
  "name": "reflex-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for the Reflex autonomous incident-to-fix pipeline",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
